# CRUD-FLASK

Pequeno exemplo de lista de tarefas com Flask.

Instalação e execução:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

Abra: http://127.0.0.1:5000
